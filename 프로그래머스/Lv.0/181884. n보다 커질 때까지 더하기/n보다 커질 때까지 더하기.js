function solution(numbers, n) {
    let answer = 0;
    for (number of numbers) {
        if (answer > n) {
            return answer
        } else {
            answer += number
        }
    }
    return answer;
}